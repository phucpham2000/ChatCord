{

    "version": "1.0",
    "configurations": [
        {
            "type": "pwa-chrome",
            "request": "launch",
            "name": "Chat App",
            "url": "http://localhost:8080",
            "webRoot": "${workspaceFolder}"
        }
    ]
}